// Password reset flow (Auth step)
export default function ForgotPasswordPage() {
  return (
    <div className="flex min-h-screen items-center justify-center text-slate-400">
      ForgotPasswordPage — coming in a later build step
    </div>
  );
}
