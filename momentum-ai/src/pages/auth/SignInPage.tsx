// Google + email sign-in (Auth step)
export default function SignInPage() {
  return (
    <div className="flex min-h-screen items-center justify-center text-slate-400">
      SignInPage — coming in a later build step
    </div>
  );
}
