// Today's tasks, AI suggestions, productivity score (Dashboard step)
export default function DashboardPage() {
  return (
    <div className="flex min-h-screen items-center justify-center text-slate-400">
      DashboardPage — coming in a later build step
    </div>
  );
}
