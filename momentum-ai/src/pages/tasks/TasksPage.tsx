// Full task list and CRUD (Dashboard step)
export default function TasksPage() {
  return (
    <div className="flex min-h-screen items-center justify-center text-slate-400">
      TasksPage — coming in a later build step
    </div>
  );
}
