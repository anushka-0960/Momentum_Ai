// Implemented in the Auth step: signInWithGoogle, signInWithEmail,
// signUp, signOutUser, sendPasswordReset — all thin wrappers around
// the Firebase Auth SDK, kept here so components never import
// "firebase/auth" directly.
export {};
