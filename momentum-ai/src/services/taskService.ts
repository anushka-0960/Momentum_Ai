// Implemented in the Dashboard + Task CRUD step: createTask, updateTask,
// deleteTask, and subscribeToTasks (Firestore onSnapshot), per the
// "client SDK for CRUD" decision in the architecture doc.
export {};
