import { Router } from "express";

// Implemented in the Analytics step: aggregates focusSessions and
// task history into weekly analyticsSnapshots documents.
export const analyticsRouter = Router();
